type skillInfo = {
    category: string;
    skills: skill[];
}

type skill = {
    name: string;
    icon: string;
}

const skillList: skillInfo[] = [
    {
        category: "Languages",
        skills: [
            
        ]
    }
]

const Skills = () => {
    return (<></>);
}

export default Skills;